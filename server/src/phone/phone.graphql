type Phone {
  id: ID!
  countryCode: Int!
  number: Int!
}

input PhoneInput {
  id: ID!
  countryCode: Int!
  number: Int!
}

input CreatePhoneInput {
  countryCode: Int!
  number: Int!
}

input UpdatePhoneInput {
  countryCode: Int
  number: Int
}
