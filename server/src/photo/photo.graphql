type Photo {
  id: ID!
  externalId: String!
  name: String!
  url: String!
  isDeleted: Boolean!
}

input PhotoInput {
  id: ID!
  externalId: String!
  name: String!
  url: String!
  isDeleted: Boolean!
}

input CreatePhotoInput {
  externalId: String!
  name: String!
  url: String!
}

input UpdatePhotoInput {
  externalId: String!
  name: String!
  url: String!
}

input MarkAsDeletePhotoInput {
  id: ID!
}
