type Passport {
  id: ID!
  identifier: String!
  uNumber: String!
  pNumber: String!
  nationalityCode: String!
  issueDate: String!
  expirationDate: String!
  type: String!
  placeOfBirth: String!
  user: User
}

input UpdatePassportInput {
  nationalityCode: String!
  placeOfBirth: String!
  publicKey: String!
}
