type Passport {
  id: ID!
  mrz: String!
  uNumber: String!
  pNumber: String!
  nationalityCode: String!
  issueDate: String!
  expirationDate: String!
  type: String!
  placeOfBirth: String!
  user: User
}

input UpdatePassportInput {
  nationalityCode: String!
  placeOfBirth: String!
  publicKey: String!
}
